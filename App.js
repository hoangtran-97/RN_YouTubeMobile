import React from "react";
import {
    SafeAreaView,
    StyleSheet,
    View,
    Text,
    StatusBar
} from "react-native";

const App = () => (
    <>
        <SafeAreaView >
            <Text >Test</Text>
        </SafeAreaView>
    </>
);

const styles = StyleSheet.create({
    RenderName: {
        height: 100
    },
    header: {
        width: 100
    }
});

export default App;
